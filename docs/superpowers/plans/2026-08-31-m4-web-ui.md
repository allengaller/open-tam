# M4 Web UI 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 open-tam 新增 FastAPI 服务 + 单页聊天界面，排查过程经 SSE 流式展示（M4 里程碑验收：浏览器发起排查并看到流式过程）。

**Architecture:** `TraceRecorder` 加 sink 回调（trace JSONL 仍是唯一事实源，流式为旁路广播）；CLI `investigate` 组装主体抽为 `orchestrator/investigate.py:run_investigation`（CLI 与 Web 共用）；`web/` 包提供 InvestigationHub（内存会话注册表 + 线程→asyncio 事件桥接）、FastAPI API（SSE 推流）、单文件前端（无构建）。

**Tech Stack:** FastAPI + uvicorn（已在依赖）、SSE（StreamingResponse）、vanilla JS EventSource、pytest + httpx/TestClient。

**设计文档：** `docs/superpowers/specs/2026-08-31-m4-web-ui-design.md`（已提交 af9f188）

**环境注意（必读）：**
- 跑测试：`UV_NO_EDITABLE=1 uv run pytest -q`（**不要用管道**，管道吞退出码）
- 跑 CLI 验收前：`UV_NO_EDITABLE=1 uv sync --reinstall-package open-tam`（非 editable 安装的旧快照坑）
- 测试隔离：`monkeypatch.setenv("OPEN_TAM_STATE_DIR"/"OPEN_TAM_REPORTS_DIR"/"OPEN_TAM_TRACES_DIR", ...)`
- 每次提交只 `git add` 计划列出的文件；工作树有大量外来脏文件（.impeccable/、DESIGN.md 等）绝不能带入

---

### Task 1: TraceRecorder sink 回调

**Files:**
- Modify: `src/open_tam/tracing/trace.py`
- Test: `tests/test_trace.py`（追加）

- [ ] **Step 1: 写失败测试**

在 `tests/test_trace.py` 末尾追加：

```python
def test_record_invokes_sink(tmp_path):
    from open_tam.tracing.trace import TraceRecorder, load_trace

    seen: list[dict] = []
    trace = TraceRecorder(alert_id="a1", traces_dir=tmp_path, sink=seen.append)
    trace.record("tool_call", step=0, tool="query_metrics", arguments={"m": 1})
    trace.record("final", content="done")

    assert [e["kind"] for e in seen] == ["tool_call", "final"]
    assert seen[0]["tool"] == "query_metrics"
    # JSONL 落盘行为不变，且 sink 收到的 entry 与文件内容一致
    on_disk = load_trace(tmp_path / "a1.jsonl")
    assert [e["kind"] for e in on_disk] == ["tool_call", "final"]
    assert seen[0] == on_disk[0]


def test_record_without_sink_unchanged(tmp_path):
    from open_tam.tracing.trace import TraceRecorder, load_trace

    trace = TraceRecorder(alert_id="a2", traces_dir=tmp_path)
    trace.record("final", content="ok")
    assert load_trace(tmp_path / "a2.jsonl")[0]["kind"] == "final"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `UV_NO_EDITABLE=1 uv run pytest tests/test_trace.py -q`
Expected: FAIL，`TypeError: TraceRecorder.__init__() got an unexpected keyword argument 'sink'`

- [ ] **Step 3: 实现 sink**

`src/open_tam/tracing/trace.py` 改为：

```python
from __future__ import annotations

import json
import os
from datetime import datetime
from pathlib import Path
from typing import Callable


class TraceRecorder:
    """排查过程逐条追加 JSONL：每行 {ts, alert_id, kind, ...fields}，可回放。

    sink：可选旁路回调（如 Web 流式广播），record 时在 JSONL 落盘后同步调用。
    trace 文件仍是唯一事实源。
    """

    def __init__(
        self,
        alert_id: str,
        traces_dir: Path | str | None = None,
        agent: str | None = None,
        sink: Callable[[dict], None] | None = None,
    ) -> None:
        base = (
            Path(traces_dir)
            if traces_dir
            else Path(os.environ.get("OPEN_TAM_TRACES_DIR", "traces"))
        )
        base.mkdir(parents=True, exist_ok=True)
        self.alert_id = alert_id
        self.agent = agent
        self.sink = sink
        self.path = base / f"{alert_id}.jsonl"

    def record(self, kind: str, **fields: object) -> None:
        entry: dict = {
            "ts": datetime.now().isoformat(timespec="milliseconds"),
            "alert_id": self.alert_id,
            "kind": kind,
        }
        if self.agent:
            entry["agent"] = self.agent
        entry.update(fields)
        with self.path.open("a", encoding="utf-8") as f:
            f.write(json.dumps(entry, ensure_ascii=False) + "\n")
        if self.sink:
            self.sink(entry)


def load_trace(path: Path | str) -> list[dict]:
    text = Path(path).read_text(encoding="utf-8")
    return [json.loads(line) for line in text.splitlines() if line.strip()]
```

- [ ] **Step 4: 跑测试确认通过**

Run: `UV_NO_EDITABLE=1 uv run pytest tests/test_trace.py -q`
Expected: PASS（含既有用例）

- [ ] **Step 5: 提交**

```bash
git add src/open_tam/tracing/trace.py tests/test_trace.py
git commit -m "feat: TraceRecorder 支持 sink 旁路回调（Web 流式广播入口）"
```

---

### Task 2: 抽取 run_investigation（CLI 与 Web 共用组装）

**Files:**
- Create: `src/open_tam/orchestrator/investigate.py`
- Modify: `src/open_tam/cli.py`（investigate 命令瘦身为薄壳）
- Test: 无新测试——重构由现有 `tests/test_investigate.py`、`tests/test_investigate_guardrails.py` 回归保障

- [ ] **Step 1: 新建 orchestrator/investigate.py**

内容（组装主体原样从 cli.py 搬入，trace 由调用方注入）：

```python
from __future__ import annotations

import os
from datetime import datetime, timedelta

from open_tam.actions import ACTION_REGISTRY
from open_tam.config import Settings
from open_tam.guardrails import AuditLogger, AutoDeny, Guardrails
from open_tam.models import AlertEvent
from open_tam.orchestrator.agents import (
    LOG_AGENT_PROMPT,
    METRIC_AGENT_PROMPT,
    ORCHESTRATOR_PROMPT,
    AgentBackend,
    SpecialistAgent,
)
from open_tam.orchestrator.loop import (
    DiagnosisResult,
    FakeChatModel,
    ModelReply,
    ReActLoop,
    ToolCall,
)
from open_tam.orchestrator.tools import (
    ORCHESTRATOR_TOOLS,
    QUERY_LOGS_SPEC,
    QUERY_METRICS_SPEC,
    GuardrailsBackend,
    InlineBackend,
    McpStdioBackend,
)
from open_tam.reporting.report import save_report
from open_tam.tracing.trace import TraceRecorder


def run_investigation(
    alert: AlertEvent,
    settings: Settings,
    *,
    fake: bool = False,
    transport: str = "inline",
    trace: TraceRecorder,
    metric_trace: TraceRecorder,
    log_trace: TraceRecorder,
) -> tuple[DiagnosisResult, object]:
    """orchestrator 双子 Agent 排查闭环：运行 ReActLoop 并落盘报告。

    返回 (DiagnosisResult, 报告路径)。trace 三个 recorder 由调用方创建
    （CLI 不传 sink；Web 传 sink 做流式广播）。
    """
    leaf_backend = InlineBackend() if transport == "inline" else McpStdioBackend()

    now = datetime.now().replace(second=0, microsecond=0)
    window = {"start": (now - timedelta(minutes=60)).isoformat(), "end": now.isoformat()}

    if fake:
        orch_model = FakeChatModel([
            ModelReply(content="先问指标子 Agent 确认异常", tool_calls=[ToolCall(
                id="t1", name="ask_metric_agent",
                arguments={"question": f"{alert.service} 的 {alert.metric} 最近一小时是否异常？异常窗口与幅度？"})]),
            ModelReply(content="再向日志子 Agent 要现场证据", tool_calls=[ToolCall(
                id="t2", name="ask_log_agent",
                arguments={"question": f"检索 {alert.service} 最近一小时 ERROR/WARN 日志，找与 {alert.metric} 异常相关的证据"})]),
            ModelReply(content=(
                '```json\n{"root_cause": "demo-app /search 接口低效正则导致 CPU 飙升", '
                '"evidence": ["指标子 Agent：cpu_usage 持续高于 85", "日志子 Agent 返回的现场证据"], '
                '"actions": ["回滚最近发布", "优化正则逻辑"], "confidence": "high"}\n```'),
                tool_calls=[]),
        ])
        metric_agent = SpecialistAgent(
            name="metric", system_prompt=METRIC_AGENT_PROMPT,
            tools=[QUERY_METRICS_SPEC], backend=leaf_backend,
            model=FakeChatModel([ModelReply(content="cpu_usage 在最近 30 分钟持续高于 85，确认异常")]),
        )
        log_agent = SpecialistAgent(
            name="log", system_prompt=LOG_AGENT_PROMPT,
            tools=[QUERY_LOGS_SPEC], backend=leaf_backend,
            model=FakeChatModel([
                ModelReply(content=None, tool_calls=[ToolCall(id="l1", name="query_logs", arguments={
                    "service": alert.service, "start": window["start"], "end": window["end"],
                })]),
                ModelReply(content="最近一小时无 ERROR 级日志，异常主要体现在指标层"),
            ]),
        )
    else:
        if not os.environ.get("DASHSCOPE_API_KEY"):
            raise RuntimeError("未设置 DASHSCOPE_API_KEY。真实排查需配置 Key，或使用 fake 演示路径。")
        from open_tam.orchestrator.llm import AgentScopeChatModel

        llm = AgentScopeChatModel(
            primary=settings.model_primary, fallback=settings.model_fallback
        )
        orch_model = llm
        metric_agent = SpecialistAgent(
            name="metric", system_prompt=METRIC_AGENT_PROMPT,
            tools=[QUERY_METRICS_SPEC], backend=leaf_backend, model=llm,
            trace=metric_trace,
        )
        log_agent = SpecialistAgent(name="log", system_prompt=LOG_AGENT_PROMPT,
                                    tools=[QUERY_LOGS_SPEC], backend=leaf_backend, model=llm,
                                    trace=log_trace)

    backend = AgentBackend({
        "ask_metric_agent": metric_agent,
        "ask_log_agent": log_agent,
    })
    guardrails = Guardrails(ACTION_REGISTRY, AuditLogger(settings.state_dir),
                            confirmer=AutoDeny())
    backend = GuardrailsBackend(backend, guardrails, actor="agent")
    loop = ReActLoop(model=orch_model, backend=backend, max_steps=settings.max_steps,
                     char_budget=settings.char_budget, system_prompt=ORCHESTRATOR_PROMPT,
                     tools=ORCHESTRATOR_TOOLS, trace=trace)
    result = loop.run(alert)
    path = save_report(alert, result, reports_dir=settings.reports_dir)
    return result, path
```

- [ ] **Step 2: cli.py 的 investigate 命令改为薄壳**

`src/open_tam/cli.py` 中 `investigate` 函数体替换为（import 块整体替换）：

```python
@app.command("investigate")
def investigate(
    alert_file: str = typer.Option(..., help="告警 JSON 文件路径"),
    fake: bool = typer.Option(False, help="使用内置 FakeChatModel（无 Key 演示）"),
    transport: str = typer.Option("inline", help="inline 或 mcp"),
) -> None:
    """读取告警 JSON，orchestrator 委托双子 Agent 排查并生成结构化根因报告与 trace。"""
    from open_tam.config import Settings
    from open_tam.orchestrator.investigate import run_investigation
    from open_tam.receiver.alert_receiver import normalize_alert
    from open_tam.tracing.trace import TraceRecorder

    settings = Settings.load()
    raw = json.loads(Path(alert_file).read_text(encoding="utf-8"))
    alert = normalize_alert(raw)
    trace = TraceRecorder(alert_id=alert.alert_id, traces_dir=settings.traces_dir)
    metric_trace = TraceRecorder(alert_id=alert.alert_id, traces_dir=settings.traces_dir, agent="metric")
    log_trace = TraceRecorder(alert_id=alert.alert_id, traces_dir=settings.traces_dir, agent="log")
    try:
        result, path = run_investigation(
            alert, settings, fake=fake, transport=transport,
            trace=trace, metric_trace=metric_trace, log_trace=log_trace,
        )
    except RuntimeError as exc:
        typer.echo(f"错误：{exc}", err=True)
        raise typer.Exit(1)
    typer.echo(f"report saved: {path}")
    typer.echo(f"trace saved: {trace.path}")
    typer.echo(result.root_cause or "未定位根因")
```

注意：原函数体内从 `from open_tam.config import Settings` 到 `typer.echo(result.root_cause or "未定位根因")` 的整段（含 fake/真实两个分支、GuardrailsBackend 接线）全部删除，由上面这段替换。原真实分支里 `if not os.environ.get("DASHSCOPE_API_KEY")` 的报错逻辑已移入 `run_investigation`（RuntimeError），CLI 捕获后保持原有退出码 1 行为。

- [ ] **Step 3: 回归测试**

Run: `UV_NO_EDITABLE=1 uv run pytest tests/test_investigate.py tests/test_investigate_guardrails.py -q`
Expected: 全部 PASS（重构不改变行为）

- [ ] **Step 4: 全量回归**

Run: `UV_NO_EDITABLE=1 uv run pytest -q`
Expected: 92 passed + 1 skipped（与重构前一致）

- [ ] **Step 5: 提交**

```bash
git add src/open_tam/orchestrator/investigate.py src/open_tam/cli.py
git commit -m "refactor: 抽取 run_investigation 排查组装主体（CLI 与 Web 共用）"
```

---

### Task 3: InvestigationHub（会话注册表 + 事件广播）

**Files:**
- Create: `src/open_tam/web/__init__.py`（空文件）
- Create: `src/open_tam/web/events.py`
- Test: `tests/test_web_events.py`

- [ ] **Step 1: 写失败测试**

新建 `tests/test_web_events.py`：

```python
import asyncio

from open_tam.models import AlertEvent
from open_tam.web.events import InvestigationHub


def _alert() -> AlertEvent:
    return AlertEvent(alert_name="CPU使用率过高", service="demo-app",
                      metric="cpu_usage", threshold=80, current_value=92.5)


def test_hub_create_and_broadcast():
    hub = InvestigationHub()
    inv = hub.create(_alert(), fake=True, transport="inline")
    assert inv.status == "pending"
    assert hub.get(inv.id) is inv

    loop = asyncio.new_event_loop()
    queue = asyncio.Queue()
    sub = hub.subscribe(inv.id, queue=queue, loop=loop)
    assert sub.snapshot == []

    sink = hub.sink_for(inv.id)
    sink({"kind": "tool_call", "tool": "ask_metric_agent"})
    sink({"kind": "final", "content": "done"})

    assert [e["kind"] for e in inv.events] == ["tool_call", "final"]
    assert [e["seq"] for e in inv.events] == [0, 1]
    # 广播经 loop.call_soon_threadsafe 进入 queue
    e1 = loop.run_until_complete(asyncio.wait_for(queue.get(), timeout=1))
    e2 = loop.run_until_complete(asyncio.wait_for(queue.get(), timeout=1))
    assert (e1["kind"], e2["kind"]) == ("tool_call", "final")
    loop.close()


def test_hub_subscribe_replays_snapshot():
    hub = InvestigationHub()
    inv = hub.create(_alert(), fake=True, transport="inline")
    sink = hub.sink_for(inv.id)
    sink({"kind": "alert_received"})

    loop = asyncio.new_event_loop()
    sub = hub.subscribe(inv.id, queue=asyncio.Queue(), loop=loop)
    assert [e["kind"] for e in sub.snapshot] == ["alert_received"]
    # 订阅后新事件只进 queue，不进 snapshot
    sink({"kind": "final", "content": "x"})
    assert len(sub.snapshot) == 1
    e = loop.run_until_complete(asyncio.wait_for(sub.queue.get(), timeout=1))
    assert e["kind"] == "final" and e["seq"] == 1
    loop.close()


def test_hub_finish_marks_done_and_broadcasts_terminal():
    hub = InvestigationHub()
    inv = hub.create(_alert(), fake=True, transport="inline")
    loop = asyncio.new_event_loop()
    sub = hub.subscribe(inv.id, queue=asyncio.Queue(), loop=loop)

    hub.finish(inv.id, report_path="reports/x.md", root_cause="正则回溯",
               confidence="high", evidence=["e1"], actions=["a1"])
    assert inv.status == "done"
    e = loop.run_until_complete(asyncio.wait_for(sub.queue.get(), timeout=1))
    assert e["kind"] == "done" and e["report_path"] == "reports/x.md"
    loop.close()


def test_hub_fail_marks_error():
    hub = InvestigationHub()
    inv = hub.create(_alert(), fake=True, transport="inline")
    loop = asyncio.new_event_loop()
    sub = hub.subscribe(inv.id, queue=asyncio.Queue(), loop=loop)

    hub.fail(inv.id, "boom")
    assert inv.status == "error"
    e = loop.run_until_complete(asyncio.wait_for(sub.queue.get(), timeout=1))
    assert e["kind"] == "error" and e["error"] == "boom"
    loop.close()


def test_hub_unknown_id_raises():
    hub = InvestigationHub()
    try:
        hub.sink_for("nope")
    except KeyError:
        pass
    else:
        raise AssertionError("expected KeyError")
```

- [ ] **Step 2: 跑测试确认失败**

Run: `UV_NO_EDITABLE=1 uv run pytest tests/test_web_events.py -q`
Expected: FAIL，`ModuleNotFoundError: No module named 'open_tam.web'`

- [ ] **Step 3: 实现 web 包与 events.py**

新建 `src/open_tam/web/__init__.py`（空文件）。

新建 `src/open_tam/web/events.py`：

```python
from __future__ import annotations

import asyncio
import threading
from dataclasses import dataclass, field
from uuid import uuid4

from open_tam.models import AlertEvent


@dataclass
class Subscription:
    """一次 SSE 订阅：先重放 snapshot（订阅前已发生的事件），再消费 queue 增量。"""

    snapshot: list[dict]
    queue: asyncio.Queue


@dataclass
class Investigation:
    id: str
    alert: AlertEvent
    fake: bool
    transport: str
    status: str = "pending"  # pending | running | done | error
    events: list[dict] = field(default_factory=list)
    subscribers: list[tuple[asyncio.Queue, asyncio.AbstractEventLoop]] = field(default_factory=list)
    lock: threading.Lock = field(default_factory=threading.Lock)


class InvestigationHub:
    """排查会话注册表 + 事件广播（单进程内存态，重启即清）。

    TraceRecorder 的 sink 在后台排查线程内被调用：加锁 append 事件缓存（赋 seq），
    再经 loop.call_soon_threadsafe 桥接进每个订阅者的 asyncio.Queue。
    """

    def __init__(self) -> None:
        self._investigations: dict[str, Investigation] = {}
        self._lock = threading.Lock()

    def create(self, alert: AlertEvent, *, fake: bool, transport: str) -> Investigation:
        inv = Investigation(id=uuid4().hex[:12], alert=alert, fake=fake, transport=transport)
        with self._lock:
            self._investigations[inv.id] = inv
        return inv

    def get(self, investigation_id: str) -> Investigation | None:
        return self._investigations.get(investigation_id)

    def sink_for(self, investigation_id: str):
        inv = self._investigations[investigation_id]

        def sink(entry: dict) -> None:
            with inv.lock:
                entry = {**entry, "seq": len(inv.events)}
                inv.events.append(entry)
                subscribers = list(inv.subscribers)
            for queue, loop in subscribers:
                loop.call_soon_threadsafe(queue.put_nowait, entry)

        return sink

    def subscribe(self, investigation_id: str, *, queue: asyncio.Queue,
                  loop: asyncio.AbstractEventLoop) -> Subscription:
        inv = self._investigations[investigation_id]
        with inv.lock:
            snapshot = list(inv.events)
            inv.subscribers.append((queue, loop))
        return Subscription(snapshot=snapshot, queue=queue)

    def _terminal(self, investigation_id: str, status: str, **fields: object) -> None:
        inv = self._investigations[investigation_id]
        inv.status = status
        self.sink_for(investigation_id)({"kind": status if status != "done" else "done", **fields})

    def finish(self, investigation_id: str, *, report_path: str, root_cause: str | None,
               confidence: str, evidence: list[str], actions: list[str]) -> None:
        inv = self._investigations[investigation_id]
        inv.status = "done"
        self.sink_for(investigation_id)({
            "kind": "done", "report_path": report_path, "root_cause": root_cause,
            "confidence": confidence, "evidence": evidence, "actions": actions,
        })

    def fail(self, investigation_id: str, error: str) -> None:
        inv = self._investigations[investigation_id]
        inv.status = "error"
        self.sink_for(investigation_id)({"kind": "error", "error": error})
```

（说明：`_terminal` 辅助方法若未被用到可不写；finish/fail 直接实现即可。写计划时保留 finish/fail 两个公开方法，`_terminal` 删除以避免死代码。）

- [ ] **Step 4: 跑测试确认通过**

Run: `UV_NO_EDITABLE=1 uv run pytest tests/test_web_events.py -q`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/open_tam/web/__init__.py src/open_tam/web/events.py tests/test_web_events.py
git commit -m "feat: InvestigationHub 会话注册表与事件广播（线程→asyncio 桥接）"
```

---

### Task 4: Web 排查编排 service.py

**Files:**
- Create: `src/open_tam/web/service.py`
- Test: `tests/test_web_service.py`

- [ ] **Step 1: 写失败测试**

新建 `tests/test_web_service.py`：

```python
import asyncio
import threading

from open_tam.models import AlertEvent
from open_tam.web.events import InvestigationHub
from open_tam.web.service import start_investigation


def _alert() -> AlertEvent:
    return AlertEvent(alert_name="CPU使用率过高", service="demo-app",
                      metric="cpu_usage", threshold=80, current_value=92.5)


def _wait_done(hub: InvestigationHub, inv_id: str, timeout: float = 10.0) -> None:
    import time
    deadline = time.monotonic() + timeout
    while time.monotonic() < deadline:
        status = hub.get(inv_id).status
        if status in ("done", "error"):
            return
        time.sleep(0.05)
    raise AssertionError("investigation did not finish in time")


def test_start_investigation_fake_runs_to_done(tmp_path, monkeypatch):
    monkeypatch.setenv("OPEN_TAM_STATE_DIR", str(tmp_path))
    monkeypatch.setenv("OPEN_TAM_REPORTS_DIR", str(tmp_path / "reports"))
    monkeypatch.setenv("OPEN_TAM_TRACES_DIR", str(tmp_path / "traces"))
    from open_tam.faults import FaultState
    FaultState().activate("cpu_spike", duration_minutes=30)

    hub = InvestigationHub()
    loop = asyncio.new_event_loop()
    threading.Thread(target=loop.run_forever, daemon=True).start()

    inv = start_investigation(hub, _alert(), fake=True, transport="inline", loop=loop)
    assert inv.status == "running"
    _wait_done(hub, inv.id)

    assert inv.status == "done"
    kinds = [e["kind"] for e in inv.events]
    assert kinds[0] == "alert_received"
    assert "tool_call" in kinds and "observation" in kinds and "final" in kinds
    assert kinds[-1] == "done"
    done = inv.events[-1]
    assert done["root_cause"] and done["report_path"].endswith(".md")
    # 报告真实落盘
    reports = list((tmp_path / "reports").glob("*.md"))
    assert len(reports) == 1
    loop.call_soon_threadsafe(loop.stop)


def test_start_investigation_error_marks_error(tmp_path, monkeypatch):
    monkeypatch.setenv("OPEN_TAM_STATE_DIR", str(tmp_path))
    monkeypatch.setenv("OPEN_TAM_REPORTS_DIR", str(tmp_path / "reports"))
    monkeypatch.setenv("OPEN_TAM_TRACES_DIR", str(tmp_path / "traces"))

    hub = InvestigationHub()
    loop = asyncio.new_event_loop()
    threading.Thread(target=loop.run_forever, daemon=True).start()

    # 真实路径且无 Key → run_investigation 抛 RuntimeError → fail
    monkeypatch.delenv("DASHSCOPE_API_KEY", raising=False)
    inv = start_investigation(hub, _alert(), fake=False, transport="inline", loop=loop)
    _wait_done(hub, inv.id)

    assert inv.status == "error"
    assert inv.events[-1]["kind"] == "error"
    assert "DASHSCOPE_API_KEY" in inv.events[-1]["error"]
    loop.call_soon_threadsafe(loop.stop)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `UV_NO_EDITABLE=1 uv run pytest tests/test_web_service.py -q`
Expected: FAIL，`ModuleNotFoundError: No module named 'open_tam.web.service'`

- [ ] **Step 3: 实现 service.py**

新建 `src/open_tam/web/service.py`：

```python
from __future__ import annotations

import asyncio
import threading

from open_tam.config import Settings
from open_tam.models import AlertEvent
from open_tam.orchestrator.investigate import run_investigation
from open_tam.tracing.trace import TraceRecorder
from open_tam.web.events import Investigation, InvestigationHub


def start_investigation(
    hub: InvestigationHub,
    alert: AlertEvent,
    *,
    fake: bool,
    transport: str,
    loop: asyncio.AbstractEventLoop,
) -> Investigation:
    """登记会话并在后台线程跑排查；TraceRecorder 挂 hub sink 做流式广播。

    loop：FastAPI 所在的事件循环（sink 广播经 call_soon_threadsafe 桥接进去）。
    """
    inv = hub.create(alert, fake=fake, transport=transport)
    inv.status = "running"
    threading.Thread(
        target=_run, args=(hub, inv, loop), daemon=True,
        name=f"investigate-{inv.id}",
    ).start()
    return inv


def _run(hub: InvestigationHub, inv: Investigation, loop: asyncio.AbstractEventLoop) -> None:
    settings = Settings.load()
    sink = hub.sink_for(inv.id)
    alert_id = inv.alert.alert_id
    trace = TraceRecorder(alert_id=alert_id, traces_dir=settings.traces_dir, sink=sink)
    metric_trace = TraceRecorder(alert_id=alert_id, traces_dir=settings.traces_dir,
                                 agent="metric", sink=sink)
    log_trace = TraceRecorder(alert_id=alert_id, traces_dir=settings.traces_dir,
                              agent="log", sink=sink)
    try:
        result, path = run_investigation(
            inv.alert, settings, fake=inv.fake, transport=inv.transport,
            trace=trace, metric_trace=metric_trace, log_trace=log_trace,
        )
    except Exception as exc:
        hub.fail(inv.id, f"{type(exc).__name__}: {exc}")
        return
    hub.finish(inv.id, report_path=str(path), root_cause=result.root_cause,
               confidence=result.confidence, evidence=result.evidence,
               actions=result.actions)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `UV_NO_EDITABLE=1 uv run pytest tests/test_web_service.py -q`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/open_tam/web/service.py tests/test_web_service.py
git commit -m "feat: Web 排查编排 start_investigation（后台线程 + sink 广播 + done/error 终态）"
```

---

### Task 5: FastAPI 应用与 API（含 SSE 端点）

**Files:**
- Create: `src/open_tam/web/app.py`
- Create: `src/open_tam/web/static/index.html`（本任务先放占位页，Task 6 写完整前端）
- Test: `tests/test_web_app.py`

- [ ] **Step 1: 写失败测试**

新建 `tests/test_web_app.py`：

```python
import json

import pytest
from httpx import ASGITransport, AsyncClient

from open_tam.web.app import create_app


@pytest.fixture
def app(tmp_path, monkeypatch):
    monkeypatch.setenv("OPEN_TAM_STATE_DIR", str(tmp_path))
    monkeypatch.setenv("OPEN_TAM_REPORTS_DIR", str(tmp_path / "reports"))
    monkeypatch.setenv("OPEN_TAM_TRACES_DIR", str(tmp_path / "traces"))
    return create_app()


async def test_post_investigation_invalid_alert_422(app):
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://t") as c:
        r = await c.post("/api/investigations", json={"alert": {"bad": 1}})
    assert r.status_code == 422


async def test_post_investigation_fake_streams_to_done(app):
    from open_tam.faults import FaultState
    FaultState().activate("cpu_spike", duration_minutes=30)
    alert = {"alert_name": "CPU使用率过高", "service": "demo-app",
             "metric": "cpu_usage", "threshold": 80, "current_value": 92.5}

    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://t") as c:
        r = await c.post("/api/investigations", json={"alert": alert, "fake": True})
        assert r.status_code == 202
        inv_id = r.json()["id"]

        kinds: list[str] = []
        async with c.stream("GET", f"/api/investigations/{inv_id}/events") as s:
            assert s.status_code == 200
            assert s.headers["content-type"].startswith("text/event-stream")
            async for line in s.aiter_lines():
                if not line.startswith("data:"):
                    continue
                event = json.loads(line[len("data:"):].strip())
                kinds.append(event["kind"])
                if event["kind"] in ("done", "error"):
                    break

    assert kinds[0] == "alert_received"
    assert "tool_call" in kinds and "observation" in kinds and "final" in kinds
    assert kinds[-1] == "done"


async def test_events_unknown_id_404(app):
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://t") as c:
        r = await c.get("/api/investigations/nope/events")
    assert r.status_code == 404


async def test_faults_list_and_activate(app):
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://t") as c:
        r = await c.get("/api/faults")
        assert r.status_code == 200
        names = [f["name"] for f in r.json()["faults"]]
        assert "cpu_spike" in names

        r = await c.post("/api/faults/cpu_spike/activate")
        assert r.status_code == 200 and r.json()["activated"] == "cpu_spike"

        from open_tam.faults import FaultState
        assert FaultState().is_active("cpu_spike")

        r = await c.post("/api/faults/nope/activate")
        assert r.status_code == 404


async def test_index_served(app):
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://t") as c:
        r = await c.get("/")
    assert r.status_code == 200
    assert "open-tam" in r.text
```

- [ ] **Step 2: 跑测试确认失败**

Run: `UV_NO_EDITABLE=1 uv run pytest tests/test_web_app.py -q`
Expected: FAIL，`ModuleNotFoundError: No module named 'open_tam.web.app'`

- [ ] **Step 3: 实现 app.py 与占位 index.html**

新建 `src/open_tam/web/app.py`：

```python
from __future__ import annotations

import asyncio
import json
from pathlib import Path

from fastapi import FastAPI, HTTPException
from fastapi.responses import FileResponse, StreamingResponse

from open_tam.faults import FAULT_MODES, FaultState
from open_tam.receiver.alert_receiver import normalize_alert
from open_tam.web.events import InvestigationHub
from open_tam.web.service import start_investigation

STATIC_DIR = Path(__file__).parent / "static"


def create_app() -> FastAPI:
    app = FastAPI(title="open-tam web")
    hub = InvestigationHub()

    @app.post("/api/investigations", status_code=202)
    async def post_investigation(payload: dict) -> dict:
        if not isinstance(payload, dict) or "alert" not in payload:
            raise HTTPException(status_code=422, detail="payload must contain 'alert'")
        try:
            alert = normalize_alert(payload["alert"])
        except Exception as exc:
            raise HTTPException(status_code=422, detail=str(exc)) from exc
        loop = asyncio.get_running_loop()
        inv = start_investigation(
            hub, alert,
            fake=bool(payload.get("fake", False)),
            transport=str(payload.get("transport", "inline")),
            loop=loop,
        )
        return {"id": inv.id, "status": inv.status}

    @app.get("/api/investigations/{investigation_id}/events")
    async def investigation_events(investigation_id: str) -> StreamingResponse:
        if hub.get(investigation_id) is None:
            raise HTTPException(status_code=404, detail="unknown investigation")
        queue: asyncio.Queue = asyncio.Queue()
        loop = asyncio.get_running_loop()
        sub = hub.subscribe(investigation_id, queue=queue, loop=loop)

        async def stream():
            for event in sub.snapshot:
                yield f"data: {json.dumps(event, ensure_ascii=False)}\n\n"
                if event["kind"] in ("done", "error"):
                    return
            while True:
                try:
                    event = await asyncio.wait_for(queue.get(), timeout=15)
                except TimeoutError:
                    yield ": keep-alive\n\n"
                    continue
                yield f"data: {json.dumps(event, ensure_ascii=False)}\n\n"
                if event["kind"] in ("done", "error"):
                    return

        return StreamingResponse(stream(), media_type="text/event-stream")

    @app.get("/api/faults")
    async def list_faults() -> dict:
        return {"faults": [
            {"name": m.name, "service": m.service, "metric": m.metric,
             "anomaly_desc": m.anomaly_desc}
            for m in FAULT_MODES.values()
        ]}

    @app.post("/api/faults/{name}/activate")
    async def activate_fault(name: str) -> dict:
        if name not in FAULT_MODES:
            raise HTTPException(status_code=404, detail=f"unknown fault: {name}")
        FaultState().activate(name, duration_minutes=30)
        return {"activated": name}

    @app.get("/")
    async def index() -> FileResponse:
        return FileResponse(STATIC_DIR / "index.html")

    return app
```

新建 `src/open_tam/web/static/index.html`（占位，Task 6 替换为完整前端）：

```html
<!doctype html>
<html lang="zh">
<head><meta charset="utf-8"><title>open-tam</title></head>
<body>open-tam web ui（占位，Task 6 实现）</body>
</html>
```

- [ ] **Step 4: 跑测试确认通过**

Run: `UV_NO_EDITABLE=1 uv run pytest tests/test_web_app.py -q`
Expected: PASS（5 个用例）

- [ ] **Step 5: 提交**

```bash
git add src/open_tam/web/app.py src/open_tam/web/static/index.html tests/test_web_app.py
git commit -m "feat: FastAPI Web API（investigations SSE 流式端点 + faults 注入 + 静态页托管）"
```

---

### Task 6: 单文件前端 index.html

**Files:**
- Modify: `src/open_tam/web/static/index.html`（占位页替换为完整实现）
- Test: 无自动化测试（前端渲染逻辑）；由 Task 7 手动验收覆盖

- [ ] **Step 1: 实现完整 index.html**

替换 `src/open_tam/web/static/index.html` 全部内容：

```html
<!doctype html>
<html lang="zh">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>open-tam · SRE Agent</title>
<style>
  :root { --bg:#f6f7f9; --card:#fff; --line:#e3e6ea; --ink:#1c2330; --dim:#6b7280;
          --orch:#2563eb; --metric:#0d9488; --log:#b45309; --err:#dc2626; }
  * { box-sizing: border-box; }
  body { margin:0; font:14px/1.6 -apple-system,"PingFang SC","Microsoft YaHei",sans-serif;
         background:var(--bg); color:var(--ink); }
  header { padding:12px 20px; background:var(--card); border-bottom:1px solid var(--line);
           display:flex; align-items:center; gap:12px; flex-wrap:wrap; }
  header h1 { font-size:16px; margin:0; }
  .fault-btn { border:1px solid var(--line); background:#fff; border-radius:6px;
               padding:4px 10px; cursor:pointer; font-size:13px; }
  .fault-btn:hover { border-color:var(--orch); color:var(--orch); }
  main { max-width:880px; margin:0 auto; padding:20px; display:flex; flex-direction:column;
         gap:12px; min-height:calc(100vh - 180px); }
  .msg { background:var(--card); border:1px solid var(--line); border-radius:10px;
         padding:12px 14px; }
  .msg.user { border-left:3px solid var(--dim); }
  .msg .who { font-size:12px; color:var(--dim); margin-bottom:4px; }
  .msg pre { margin:6px 0 0; padding:8px; background:#f1f3f5; border-radius:6px;
             overflow:auto; font-size:12px; white-space:pre-wrap; word-break:break-all; }
  .ev { border-left:3px solid var(--orch); }
  .ev.metric { border-left-color:var(--metric); }
  .ev.log { border-left-color:var(--log); }
  .ev.error, .msg.report.error { border-left-color:var(--err); }
  .ev .tool { font-family:ui-monospace,monospace; font-size:12px; }
  details { margin-top:6px; } summary { cursor:pointer; color:var(--dim); font-size:12px; }
  .msg.report { border-left:3px solid var(--orch); }
  .msg.report h3 { margin:0 0 6px; font-size:15px; }
  .msg.report .conf { font-size:12px; color:var(--dim); }
  footer { max-width:880px; margin:0 auto; padding:0 20px 20px; width:100%; }
  .composer { background:var(--card); border:1px solid var(--line); border-radius:10px;
              padding:12px; display:flex; flex-direction:column; gap:8px; }
  textarea { width:100%; min-height:110px; border:1px solid var(--line); border-radius:6px;
             padding:8px; font:12px/1.5 ui-monospace,monospace; resize:vertical; }
  .row { display:flex; align-items:center; gap:14px; flex-wrap:wrap; }
  .row label { font-size:13px; color:var(--dim); display:flex; align-items:center; gap:4px; }
  button.send { background:var(--orch); color:#fff; border:0; border-radius:6px;
                padding:8px 18px; cursor:pointer; font-size:14px; }
  button.send:disabled { opacity:.5; cursor:not-allowed; }
  .hint { font-size:12px; color:var(--dim); }
</style>
</head>
<body>
<header>
  <h1>open-tam · SRE Agent 排查台</h1>
  <span class="hint">故障模板：</span>
  <span id="faults"></span>
</header>
<main id="timeline"></main>
<footer>
  <div class="composer">
    <textarea id="alert-json" placeholder='粘贴告警 JSON，或点上方故障模板自动填充'></textarea>
    <div class="row">
      <label><input type="checkbox" id="fake" checked> fake 演示（无 Key）</label>
      <label>transport
        <select id="transport"><option>inline</option><option>mcp</option></select>
      </label>
      <button class="send" id="send">发起排查</button>
      <span class="hint" id="status"></span>
    </div>
  </div>
</footer>
<script>
const timeline = document.getElementById('timeline');
const statusEl = document.getElementById('status');
const sendBtn = document.getElementById('send');
let es = null;

const AGENT_LABEL = { undefined: 'orchestrator', metric: 'metric 子 Agent', log: 'log 子 Agent' };

function el(tag, cls, text) {
  const e = document.createElement(tag);
  if (cls) e.className = cls;
  if (text !== undefined) e.textContent = text;
  return e;
}
function scrollBottom() { window.scrollTo({ top: document.body.scrollHeight, behavior: 'smooth' }); }

function addUserCard(alertJson) {
  const m = el('div', 'msg user');
  m.appendChild(el('div', 'who', '你 · 发起排查'));
  m.appendChild(el('pre', null, JSON.stringify(alertJson, null, 2)));
  timeline.appendChild(m); scrollBottom();
}

function addEvent(ev) {
  if (ev.kind === 'alert_received') return;
  if (ev.kind === 'done') return addReport(ev);
  if (ev.kind === 'error') return addError(ev);
  const agent = ev.agent;
  const cls = 'msg ev' + (agent ? ' ' + agent : '');
  const m = el('div', cls);
  m.appendChild(el('div', 'who', AGENT_LABEL[agent] || agent || 'orchestrator'));
  if (ev.kind === 'tool_call') {
    if (ev.thought) m.appendChild(el('div', null, ev.thought));
    m.appendChild(el('div', 'tool', '→ ' + ev.tool + ' ' + JSON.stringify(ev.arguments || {})));
  } else if (ev.kind === 'observation') {
    const d = el('details');
    d.appendChild(el('summary', null, '观察' + (ev.error ? '（工具错误）' : '')));
    d.appendChild(el('pre', null, ev.observation || ''));
    m.appendChild(d);
  } else if (ev.kind === 'final') {
    return; // 最终 JSON 由 done 报告卡片呈现
  } else if (ev.kind === 'budget_exceeded') {
    m.appendChild(el('div', null, '达到步数/预算上限，转人工'));
  } else {
    m.appendChild(el('pre', null, JSON.stringify(ev)));
  }
  timeline.appendChild(m); scrollBottom();
}

function addReport(ev) {
  const m = el('div', 'msg report');
  m.appendChild(el('h3', null, '根因报告'));
  m.appendChild(el('div', null, ev.root_cause || '未定位根因'));
  m.appendChild(el('div', 'conf', '置信度：' + (ev.confidence || '—') + '　报告：' + (ev.report_path || '—')));
  if (ev.evidence && ev.evidence.length) {
    const d = el('details'); d.appendChild(el('summary', null, '证据（' + ev.evidence.length + '）'));
    d.appendChild(el('pre', null, ev.evidence.join('\n'))); m.appendChild(d);
  }
  if (ev.actions && ev.actions.length) {
    const d = el('details'); d.appendChild(el('summary', null, '建议动作（' + ev.actions.length + '）'));
    d.appendChild(el('pre', null, ev.actions.join('\n'))); m.appendChild(d);
  }
  timeline.appendChild(m); scrollBottom();
  finish('排查完成');
}

function addError(ev) {
  const m = el('div', 'msg report error');
  m.appendChild(el('h3', null, '排查失败'));
  m.appendChild(el('pre', null, ev.error || 'unknown error'));
  timeline.appendChild(m); scrollBottom();
  finish('排查失败');
}

function finish(text) {
  statusEl.textContent = text;
  sendBtn.disabled = false;
  if (es) { es.close(); es = null; }
}

async function loadFaults() {
  const r = await fetch('/api/faults');
  const { faults } = await r.json();
  const box = document.getElementById('faults');
  for (const f of faults) {
    const b = el('button', 'fault-btn', f.name);
    b.title = f.anomaly_desc;
    b.onclick = () => selectFault(f);
    box.appendChild(b);
  }
}

async function selectFault(f) {
  await fetch('/api/faults/' + f.name + '/activate', { method: 'POST' });
  document.getElementById('alert-json').value = JSON.stringify({
    alert_name: f.name + ' 告警', service: f.service, metric: f.metric,
    threshold: 80, current_value: 92.5,
  }, null, 2);
  statusEl.textContent = '已注入故障 ' + f.name + '，告警 JSON 已预填';
}

sendBtn.onclick = async () => {
  let alert;
  try { alert = JSON.parse(document.getElementById('alert-json').value); }
  catch { statusEl.textContent = '告警 JSON 解析失败'; return; }
  sendBtn.disabled = true;
  statusEl.textContent = '排查中…';
  addUserCard(alert);
  const r = await fetch('/api/investigations', {
    method: 'POST', headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      alert, fake: document.getElementById('fake').checked,
      transport: document.getElementById('transport').value,
    }),
  });
  if (!r.ok) {
    const t = await r.text();
    statusEl.textContent = '发起失败：' + r.status + ' ' + t;
    sendBtn.disabled = false;
    return;
  }
  const { id } = await r.json();
  es = new EventSource('/api/investigations/' + id + '/events');
  es.onmessage = (msg) => addEvent(JSON.parse(msg.data));
  es.onerror = () => { if (es) { es.close(); es = null; } };
};

loadFaults();
</script>
</body>
</html>
```

- [ ] **Step 2: 冒烟验证页面可加载**

Run: `UV_NO_EDITABLE=1 uv run pytest tests/test_web_app.py::test_index_served -q`
Expected: PASS（页面含 open-tam 字样）

- [ ] **Step 3: 提交**

```bash
git add src/open_tam/web/static/index.html
git commit -m "feat: 单页聊天界面（故障模板 + 流式时间线 + 报告卡片，无构建）"
```

---

### Task 7: CLI `open-tam serve` 命令

**Files:**
- Modify: `src/open_tam/cli.py`（追加 serve 命令）
- Test: `tests/test_cli.py`（追加）

- [ ] **Step 1: 写失败测试**

在 `tests/test_cli.py` 末尾追加：

```python
def test_serve_command_registered():
    result = runner.invoke(app, ["serve", "--help"])
    assert result.exit_code == 0
    assert "--host" in result.output and "--port" in result.output
```

- [ ] **Step 2: 跑测试确认失败**

Run: `UV_NO_EDITABLE=1 uv run pytest tests/test_cli.py -q`
Expected: FAIL，`No such command 'serve'`

- [ ] **Step 3: 实现 serve 命令**

在 `src/open_tam/cli.py` 的 `version` 命令前追加：

```python
@app.command("serve")
def serve(
    host: str = typer.Option("127.0.0.1", help="监听地址"),
    port: int = typer.Option(8000, help="监听端口"),
) -> None:
    """启动 Web UI（FastAPI + 单页聊天界面 + SSE 流式排查过程）。"""
    import uvicorn

    from open_tam.web.app import create_app

    typer.echo(f"open-tam web ui: http://{host}:{port}")
    uvicorn.run(create_app(), host=host, port=port)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `UV_NO_EDITABLE=1 uv run pytest tests/test_cli.py -q`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add src/open_tam/cli.py tests/test_cli.py
git commit -m "feat: CLI open-tam serve 启动 Web UI"
```

---

### Task 8: M4 验收（手动演示 + 文档收尾）

**Files:**
- Modify: `README.md`（命令表加 serve 行）
- Modify: `docs/mvp-plan.md`（M4 勾选 + 验收证据）

- [ ] **Step 1: 重装包（旧快照坑）**

Run: `UV_NO_EDITABLE=1 uv sync --reinstall-package open-tam`
Expected: 成功

- [ ] **Step 2: 起服务并浏览器验收**

Run: `UV_NO_EDITABLE=1 uv run open-tam serve`（后台）
浏览器打开 `http://127.0.0.1:8000`：
1. 页面加载，顶部出现 4 个故障模板按钮
2. 点 `cpu_spike` → 状态提示已注入、告警 JSON 预填
3. 保持 fake 勾选 → 发起排查
4. 流式看到：告警卡片 → orchestrator 思考/工具调用 → 观察（可折叠）→ 子 Agent 事件（配色区分）→ 最终报告卡片（根因/证据/建议动作/置信度/报告路径）
5. 刷新页面 → 过程重放不丢（事件缓存）
6. `reports/` 落盘新报告；`open-tam trace show <alert_id>` 可回放
7. 敏感动作路径：fake 脚本不含 execute_action，可用真实模型路径或手工验证 GuardrailsBackend 拒绝事件出现在流中（本步以 fake 全流程 + 报告落盘为准）

- [ ] **Step 3: 全量回归**

Run: `UV_NO_EDITABLE=1 uv run pytest -q`
Expected: 全绿（92 + 新增用例，1 skipped）

- [ ] **Step 4: README 命令表**

在 `README.md` 命令表 `open-tam patrol watch` 行后追加：

```markdown
| `open-tam serve [--host 127.0.0.1] [--port 8000]` | 启动 Web UI（聊天式流式排查） |
```

- [ ] **Step 5: mvp-plan M4 勾选 + 验收证据**

`docs/mvp-plan.md` 的 M4 区块替换为：

```markdown
## M4 Web UI

- [x] FastAPI 服务 + 单页聊天界面
- [x] 排查过程流式展示（步骤/工具调用实时可见）
- **验收**：浏览器发起排查并看到流式过程 ✅（2026-08-31，`open-tam serve` 起服务，选 cpu_spike 模板一键注入并预填告警，fake 路径流式渲染 orchestrator/子 Agent 事件与最终报告卡片，报告落盘且 trace 可回放；刷新页面事件重放不丢；全量测试绿）
```

- [ ] **Step 6: 提交**

```bash
git add README.md docs/mvp-plan.md
git commit -m "docs: M4 验收记录（Web UI 流式排查 golden path）"
```

---

## Self-Review 记录

- **Spec 覆盖**：设计文档 §4 架构（T1 sink / T2 抽取 / T3 hub / T4 service / T5 app / T6 前端 / T7 serve）、§5.2 API 全部端点（T5）、§5.4 前端（T6）、§7 错误处理（T4 error 路径 + T5 422/404）、§8 测试策略（各任务测试 + T8 手动验收）、§9 验收标准（T8）——均有对应任务。
- **Placeholder 扫描**：无 TBD/TODO；所有代码步骤含完整代码。
- **类型一致性**：`InvestigationHub.create(alert, *, fake, transport)`、`sink_for(id)`、`subscribe(id, *, queue, loop) -> Subscription`、`finish(id, *, report_path, root_cause, confidence, evidence, actions)`、`fail(id, error)`、`start_investigation(hub, alert, *, fake, transport, loop) -> Investigation`、`run_investigation(alert, settings, *, fake, transport, trace, metric_trace, log_trace)`——T3 定义、T4/T5 使用一致。T3 计划正文已注明删除未用的 `_terminal` 辅助方法。
- **已知取舍**：SSE 测试用 httpx `stream` + `aiter_lines` 消费；`asyncio.wait_for` 超时抛 `TimeoutError`（Python 3.12 中 `asyncio.TimeoutError` 即 `TimeoutError` 别名，用内置名）。
